import {
  Container,
  CustomPaint
} from "@moonmoonbrothers/flutterjs";
export default function Line({
  thickness,
  color,
  points,
}: {
  thickness: number;
  color: string;
  points: {x: number, y: number}[]
}) {
  return Container()
}
