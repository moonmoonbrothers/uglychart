{
  "name": "@moonmoonbrothers/uglychart",
  "version": "0.0.5",
  "main": "src/index.ts",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf package && tsc --build .",
    "prepackage": "npm run build",
    "package": "cp package.json package/package.json && cd package && npm pkg delete main && npm pkg set exports='{\".\": \"./index.js\"}' --json"
  },
  "type": "module"
}
